name: Build and Publish (npm + Docker)

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - "v*.*.*"

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    env:
      IS_TAG: ${{ startsWith(github.ref, 'refs/tags/') }}
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          registry-url: "https://registry.npmjs.org"

      - name: Install root dependencies
        run: npm ci

      - name: Build Angular client and copy to src/public
        working-directory: client/dbfuse-ai-client
        run: |
          npm ci
          npm run clean-build-compress

      - name: Verify built assets exist
        run: |
          test -d src/public && ls -la src/public || (echo 'src/public missing. Did the client build step run?' && exit 1)

      - name: Configure npm auth
        if: ${{ env.IS_TAG == 'true' && env.NPM_TOKEN != '' }}
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc

      - name: Publish to npm
        if: ${{ env.IS_TAG == 'true' && env.NPM_TOKEN != '' }}
        env:
          NODE_AUTH_TOKEN: ${{ env.NPM_TOKEN }}
        run: |
          git config user.name "kshashikumar"
          git config user.email "skastu01@nyit.edu"
          npm publish --access public

      # Docker build and push
      - name: Log in to Docker Hub
        if: ${{ env.IS_TAG == 'true' && env.DOCKER_USERNAME != '' && env.DOCKER_PASSWORD != '' }}
        run: echo "${{ env.DOCKER_PASSWORD }}" | docker login -u "${{ env.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker image
        if: ${{ env.IS_TAG == 'true' && env.DOCKER_USERNAME != '' && env.DOCKER_PASSWORD != '' }}
        run: |
          IMAGE="${{ env.DOCKER_USERNAME }}/dbfuse-ai"
          TAG="${{ github.ref_name }}"
          docker build -t $IMAGE:$TAG .
          docker tag $IMAGE:$TAG $IMAGE:latest

      - name: Push Docker image
        if: ${{ env.IS_TAG == 'true' && env.DOCKER_USERNAME != '' && env.DOCKER_PASSWORD != '' }}
        run: |
          IMAGE="${{ env.DOCKER_USERNAME }}/dbfuse-ai"
          TAG="${{ github.ref_name }}"
          docker push $IMAGE:$TAG
          docker push $IMAGE:latest
