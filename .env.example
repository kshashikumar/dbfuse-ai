# Example environment variables for DBFuse AI
# Copy this file to `.env` and fill in as needed. Do NOT commit real secrets.

# Server
PORT=5000
BODY_SIZE=50mb
NODE_ENV=development

# Basic auth for UI (optional)
DBFUSE_USERNAME=admin
DBFUSE_PASSWORD=admin

# AI configuration (optional)
AI_PROVIDER=gemini
AI_MODEL=gemini-2.5-flash
AI_API_KEY=

## Note: Database connection settings are provided via the app UI.
## No DB URL env vars are used by the server. Keep secrets out of .env.
